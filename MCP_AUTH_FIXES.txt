MCP AUTHENTICATION FIXES — ACTION CHECKLIST
============================================

Current Date: 2026-03-09

BROKEN SERVERS (3) - Missing API Keys:
======================================

1. TWITTER MCP
   Status: ❌ BROKEN
   Fix: Get 4 keys from https://developer.twitter.com/
   Then add to ./.env:

   TWITTER_API_KEY=<KEY>
   TWITTER_API_SECRET=<SECRET>
   TWITTER_ACCESS_TOKEN=<TOKEN>
   TWITTER_ACCESS_TOKEN_SECRET=<SECRET>

   Test: Will fail until all 4 are added

2. LINEAR MCP
   Status: ❌ BROKEN
   Fix: Get key from https://linear.app/settings/api
   Then add to ./.env:

   LINEAR_API_KEY=<KEY>

   Test: Should work after .env is updated

3. EXA MCP
   Status: ❌ BROKEN (but enabled in settings!)
   Fix: Get key from https://dashboard.exa.ai/
   Then add to ./.env:

   EXA_API_KEY=<KEY>

   Test: Should work after .env is updated


STALE OAUTH TOKENS (3) - Need Refresh:
======================================

1. SLACK MCP
   Status: ⚠️ OAuth token stale
   Fix: Restart Claude Code
   It will auto-prompt for reauth at https://mcp.slack.com/mcp

2. GOOGLE GMAIL
   Status: ⚠️ OAuth token stale
   Fix: Restart Claude Code
   It will auto-prompt for Google reauth when you use Gmail

3. GOOGLE CALENDAR
   Status: ⚠️ OAuth token stale
   Fix: Restart Claude Code
   It will auto-prompt for Google reauth when you use Calendar


WORKING SERVERS (9) - No Action Needed:
========================================

✓ Perplexity MCP
✓ HackerNews MCP
✓ Reddit MCP
✓ mem0 (Memory)
✓ Firecrawl (Web Scraping)
✓ deep-research-mcp
✓ tree-sitter (Syntax Parsing)
✓ chrome-devtools (Browser Automation)
✓ openclaw (Custom Tools)


OPTIONAL SECURITY HARDENING:
============================

Move hardcoded keys from ./.mcp.json to .env:

Current (in .mcp.json):
  mem0: MEM0_API_KEY=m0-BN1oF7rH720Y2JmmSbGxj9cncZy4cdnS8m3UPoKyh
  firecrawl: FIRECRAWL_API_KEY=fc-aac4b5382aff41a080c103a2f1477701

Better approach:
1. Add to ./.env:
   MEM0_API_KEY=m0-BN1oF7rH720Y2JmmSbGxj9cncZy4cdnS8m3UPoKyh
   FIRECRAWL_API_KEY=fc-aac4b5382aff41a080c103a2f1477701

2. Update ./.mcp.json to use ${MEM0_API_KEY} and ${FIRECRAWL_API_KEY}

3. Prevents keys from being visible in git/backups


QUICK START:
============

1. Get the 6 missing API keys (Twitter×4, Linear, Exa)
2. Add them to ./.env
3. Restart Claude Code
4. Slack/Gmail/Calendar will auto-prompt for reauth
5. All 12 MCP servers should work

Estimated time: 30 minutes (mostly waiting for API key generation)
