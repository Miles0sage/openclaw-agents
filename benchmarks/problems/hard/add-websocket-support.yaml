id: add-websocket-support
category: feature
difficulty: hard
description: |
  Add websocket endpoint with connection manager and broadcast support.
agent_key: elite_coder_agent
max_cost_usd: 0.15
max_duration_sec: 180

setup:
  files:
    - path: src/main.py
      content: |
        from fastapi import FastAPI
        app = FastAPI()

expected:
  files:
    - path: src/main.py
      contains: ["websocket", "WebSocket", "broadcast"]
